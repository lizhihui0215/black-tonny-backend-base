# 旧 Runtime 历史归档区

这个目录预留给 `black-tonny-backend` 中已经不再适合作为新仓库真源的历史文档。

当前正式文档请先看 [docs/README.md](../../README.md)。

典型内容包括：
- 旧仓库入口文档
- 旧 split frontend/backend 运行时 API 说明
- 已被新 base 边界替代的架构叙事
- 旧协作适配文档与历史变更记录

归档规则：
- 归档文档只保留历史上下文，不参与当前实现决策
- 归档文档不能直接驱动 runtime、数据库或 API 行为
- 若后续确需复用其中某段规则，必须先改写为新仓库正式文档，再进入真源区

PR-1 只建立这个 archive 目标目录，不复制旧文档正文。
